<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kennethallan.Bloom_TB_002">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EditThemePage" />
        <activity android:name=".SetGoals" />
        <activity android:name=".AddEvent" />
        <activity android:name=".Summary" />
        <activity
            android:name=".TemporaryTestButton"
            android:label="@string/title_activity_temporary_test_button"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>